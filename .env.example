# Application
APP_URL=http://localhost
APP_ENV=development
APP_SECRET=@_leaf$0Secret!

# Database (MySQL)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=remindy
DB_USERNAME=remindy
DB_PASSWORD=remindy

# Authentication (JWT tokens)
TOKEN_LIFETIME=604800

# Mail (SMTP)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_ENCRYPTION=STARTTLS
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@example.com
MAIL_FROM_NAME=Remindy
